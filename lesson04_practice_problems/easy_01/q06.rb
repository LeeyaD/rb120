# What could we add to the class below to access the instance variable @volume?
# a getter method, this would return the value of our instance var @volume
class Cube
  def initialize(volume)
    @volume = volume
  end
end
