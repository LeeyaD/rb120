# If we have a class AngryCat how do we create a new instance of this class?
# we call #new on the class name which will return a new instance of the class
AngryCat.new
# The AngryCat class might look something like this:
class AngryCat
  def hiss
    puts "Hisssss!!!"
  end
end
